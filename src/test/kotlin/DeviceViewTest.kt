import com.sugarizer.domain.shared.JADB
import com.sugarizer.domain.shared.RxBus
import com.sugarizer.domain.shared.StringUtils
import com.sugarizer.presentation.view.device.DeviceContract
import com.sugarizer.presentation.view.device.DevicePresenter
import junit.framework.TestCase
import org.junit.Test
import org.mockito.Mockito.*

class DeviceViewTest : TestCase() {
    @Test
    fun testTest(){
        // given
//        var deviceView = mock(DeviceContract.View::class.java)
//        var jadb = mock(JADB::class.java)
//        var rxBus = mock(RxBus::class.java)
//        var stringUtils = mock(StringUtils::class.java)
//
//        var presenter: DevicePresenter = DevicePresenter(deviceView, jadb, rxBus, stringUtils)

        // when
        //presenter.start()

        // then
        //verify(deviceView, times(1)).test()
    }
}